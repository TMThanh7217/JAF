<div id="menu" class="container-fluid p-5">
    <div class="menu-label mb-3 p-2"><h1 class="ml-2">{{name}}</h1></div>
    {{#each rows}}
    <div class="row card-group">
      {{#each this}}
        {{> product-card this}}
      {{/each}}
    </div>
    {{/each}}
</div>
